<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Costume Closet</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bulma@0.8.0/css/bulma.min.css">
    <script defer src="https://use.fontawesome.com/releases/v5.3.1/js/all.js"></script>
    
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <!-- Navigation Bar -->
    <% if logged_in? %>
      <div class="tile is-ancestor">
        <div class="tile">
          <div class="tile is-parent">
            <article class="tile is-child notification is-primary">
              <nav class="level is-mobile">
                <div class="level-item has-text-centered">
                    <div>
                      <%= link_to "My Costume Closet", dance_studio_path(current_user.id), class: 'link is-info' %>
                    </div>
                </div>
                <div class="level-item has-text-centered">
                    <div>
                      <%= link_to "New Costume", new_costume_path, class: 'link is-info' %>
                    </div>
                </div>
                <div class="level-item has-text-centered">
                  <div class="field has-addons">
                    <p class="control">
                      <input class="input" type="text" placeholder="Find a costume">
                    </p>
                    <p class="control">
                      <button class="button">Search</button>
                    </p>
                  </div>
                </div>
                <div class="level-item has-text-centered">
                    <div>
                      <%= link_to "Log Out", logout_path, class: 'link is-info', method: :post %>
                    </div>
                </div>
              </nav>
            </article>
          </div>
        </div>
      </div>
    <% end %>
    <!-- Display validation messages -->
    <section class='section'>
      <div class='container'>
        <% flash.each do |name, msg| %>
          <div class='notification is-<%= name %>'>
            <%= msg %>
          </div>
        <% end %>
        <%= yield %>
      </div>
    </section>
  </body>
  <footer class="branding">
    <div class="content has-text-centered">
      <p>
      </p>
    </div>
  </footer>
</html>
